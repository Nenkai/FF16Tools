set_table_type|SingleKeyed
set_table_category|SingleKeyed
use_base_row_id|true

// Game iterates through rows for this table
// No more than 8 rows?

//////////////////////////////////
add_column|DLCFlags|int

// ref: 41 8B 40 ? 89 44 BC
add_column|Unk2|int

// ref: 8B 50 ? 48 8B 0D ? ? ? ? E8 ? ? ? ? 84 C0 74 ? 48 83 3B ? 7C ? 48 8B CB E8 ? ? ? ? 4C 8B C0
add_column|ReqUserSituationId|int
