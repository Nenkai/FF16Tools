set_table_type|SingleKeyed
set_table_category|SingleKeyed
use_base_row_id|false

add_column|DLCFlags|int

// Might just be a byte with 3 bytes padding
add_column|Unk2|int

// Vatb file to use? No clue.
// bitset 0->64
add_column|Unk3|int

// Same as above but 22 + this value?
// 0F B6 40 ? 48 83 C0 ? 48 83 F8 ? 0F 83 ? ? ? ? 48 8B C8 83 E0 ? 48 C1 E9 ? C4 C2 F9 F7 C1 48 09 44 CD ? 48 8B 5D
add_column|Unk4|byte
// <implicit 3 bytes padding>
